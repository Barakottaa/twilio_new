module.exports = {
  presets: ['module:metro-react-native-babel-preset'],
  plugins: [
    // Temporarily disabled - will add back after fixing compatibility
    // 'react-native-reanimated/plugin',
  ],
};

